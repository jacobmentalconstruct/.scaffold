# Builder Constraint Contract

_Status: active binding contract._

This file is the primary builder contract for the installed project.

- `_docs/BCC.md` is the active contract surface.
- `_docs/BCC.md` is the sole binding contract surface for the project.

If another local doctrine surface appears to conflict with `_docs/BCC.md`, prefer `_docs/BCC.md` and reconcile the derivative surface.
